import type { Settings } from "./types";

export const DEFAULT_SETTINGS: Settings = {
    catalogUrl: "/resources",
    defaultDifficulty: 3,
    preferredGenres: ["Rock", "Pop Punk"],
    repetitionsPerLine: 10,
    theme: "dark",
};
